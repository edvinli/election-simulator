__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
*.egg-info/
.DS_Store
*.swp

data/raw/pollofpolls/.staging/
data/raw/pollofpolls/*.tmp

# Wrangler local state: build cache and the resolved Cloudflare account.
# Never committed; an untracked copy also trips the capture workflow's
# clean-checkout assertion if it is left in a working tree.
.wrangler/
